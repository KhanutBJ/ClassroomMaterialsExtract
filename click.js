// Get all div elements with the specified class and attributes
var divElements = document.querySelectorAll('.jWCzBe.QRiHXd.idtp4e.u73Apc');

// Click each div element
divElements.forEach(function(divElement) {
    divElement.click();
});
